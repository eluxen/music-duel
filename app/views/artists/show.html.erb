<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>

<p>
  <strong>Default vidio:</strong>
  <%= @artist.default_song_url %>
</p>

<p>
  <strong>Profile photo url:</strong>
  <%= @artist.profile_photo_url %>
</p>
<p>
  <strong>Tags:</strong>
  <%= raw @artist.tag_list,map { |t| link_to t, tag_path(t) }.join(', ') %>
</p>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
